# Template for docker-compose secrets. Copy this file to .env and fill in real values.
# .env is gitignored (holds the real password); this example file is committed so
# teammates know the shape. Keep these in sync with appsettings.Development.json.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=KhodorPassword123!
POSTGRES_DB=BuyitDb
